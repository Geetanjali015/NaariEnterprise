
  # High-Fidelity Web UI Design

  This is a code bundle for High-Fidelity Web UI Design. The original project is available at https://www.figma.com/design/6jvg6gtuTGTrJMcMogSKeX/High-Fidelity-Web-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  